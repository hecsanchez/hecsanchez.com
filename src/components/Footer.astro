---
import { t, type Locale, contactInfo } from '../i18n';

interface Props { locale: Locale }
const { locale } = Astro.props;
const dict = t(locale);
const year = new Date().getFullYear();
const fractionalLabel = locale === 'es' ? '¿Qué es un CTO Fraccionario?' : 'What is a Fractional CTO?';
---

<footer class="border-t border-[var(--color-border)] mt-24">
  <div class="mx-auto flex max-w-5xl flex-col gap-4 px-6 py-10 text-sm text-[var(--color-fg-dim)] md:flex-row md:items-center md:justify-between">
    <div>© {year} Hec Sánchez. {dict.footer.rights}</div>
    <div class="flex flex-wrap items-center gap-x-5 gap-y-2">
      <a href={dict.routes.fractionalCto} class="hover:text-[var(--color-fg)]">{fractionalLabel}</a>
      <a href={contactInfo.linkedin} target="_blank" rel="noopener" class="hover:text-[var(--color-fg)]">LinkedIn</a>
      <a href={`mailto:${contactInfo.email}`} class="hover:text-[var(--color-fg)]">{contactInfo.email}</a>
    </div>
  </div>
</footer>
